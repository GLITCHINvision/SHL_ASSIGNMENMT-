services:
  - type: web
    name: shl-assessment-agent
    runtime: python
    pythonVersion: 3.11
    startCommand: "uvicorn main:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11
    routes:
      - path: /
        matchType: prefix
